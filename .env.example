# Supabase — Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Resend — resend.com/api-keys
RESEND_API_KEY=
REMINDER_FROM_EMAIL="Cetli <emlekezteto@your-domain.hu>"

# Public URL of this app (used in email links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Shared secret checked on /api/cron/* requests (set the same value as the
# Authorization header in vercel.json's cron config / Vercel project env)
CRON_SECRET=
